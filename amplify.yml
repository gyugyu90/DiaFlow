version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - npm ci
    build:
      commands:
        - npm test
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
