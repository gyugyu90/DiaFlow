version: 1
frontend:
  phases:
    build:
      commands:
        - 'echo "Static site build skipped"'
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - src/**/*
      - examples/**/*
